import React from "react";
import { Dimensions } from "react-native";

type Props = {};

const { width } = Dimensions.get("window");
const EcommrceHeader = (props: Props) => {
    return <></>;
};

export default EcommrceHeader;
