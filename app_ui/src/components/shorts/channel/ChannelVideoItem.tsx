import React from "react";
import { Text, View } from "react-native";

type Props = {};

const ChannelVideoItem = (props: Props) => {
    return (
        <View>
            <Text>ChannelVideoItem</Text>
        </View>
    );
};

export default ChannelVideoItem;
